$(function() {
    $( "#left" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#lefter" ).click(function() {
      $( "#left" ).dialog( "open" );
    });
    
    $( "#overtake" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#overtaker" ).click(function() {
      $( "#overtake" ).dialog( "open" );
    });
    
    $( "#speed" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#speeder" ).click(function() {
      $( "#speed" ).dialog( "open" );
    });
    
      $( "#motorways" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#motorwaysopen" ).click(function() {
      $( "#motorways" ).dialog( "open" );
    });
    
    $( "#railwaycrossing" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#railway" ).click(function() {
      $( "#railwaycrossing" ).dialog( "open" );
    });
    
      $( "#trafficlight" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#traffic" ).click(function() {
      $( "#trafficlight" ).dialog( "open" );
    });
    
          $( "#onebridge" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#oneway" ).click(function() {
      $( "#onebridge" ).dialog( "open" );
    });
    
            $( "#roundabout" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#roundabouts" ).click(function() {
      $( "#roundabout" ).dialog( "open" );
    });  
                $( "#parkiner" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#parking" ).click(function() {
      $( "#parkiner" ).dialog( "open" );
    });  
    
                    $( "#pedcrossing" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#crossings" ).click(function() {
      $( "#pedcrossing" ).dialog( "open" );
    });  
    
                        $( "#inter" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#intersection" ).click(function() {
      $( "#inter" ).dialog( "open" );
    });  
   $( "#passer" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#passing" ).click(function() {
      $( "#passer" ).dialog( "open" );
    });  
   $( "#information" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#info" ).click(function() {
      $( "#information" ).dialog( "open" );
    });  
       $( "#videoplayer" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#video" ).click(function() {
      $( "#videoplayer" ).dialog( "open" );
    });  
           $( "#slipperydialog" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#slippery" ).click(function() {
      $( "#slipperydialog" ).dialog( "open" );
    });
      $( "#turns" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#turnsopener" ).click(function() {
      $( "#turns" ).dialog( "open" );
    }); 
    
    $( "#gravel" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#gravelroads" ).click(function() {
      $( "#gravel" ).dialog( "open" );
    }); 
    
     $( "#animalsroad" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#animals" ).click(function() {
      $( "#animalsroad" ).dialog( "open" );
    }); 
    
    $( "#icyroads" ).dialog({
      autoOpen: false,
      width: 'auto', // overcomes width:'auto' and maxWidth bug
      height: 'auto',
      maxWidth: 600,
      maxHeight: 600,
      modal: true,
      fluid: true, //new option
      resizable: false,
    });
 
    $( "#ice" ).click(function() {
      $( "#icyroads" ).dialog( "open" );
    }); 
  });  